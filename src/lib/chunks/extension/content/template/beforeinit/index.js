/**
 * Called before CliNext has been launched.
 * @return {void} Always returns void.
 * @param {Object} app Express app.
 * @param {Object} schema Feature's schema.
 * @param {Object} configuration Current CliNext configuration.
 * @param {Object} server - Parse Server instance.
 * @param {Object} cliNextEngineConfig - Full cliNextEngineConfig.
 */

export default async ({ app, schema, configuration, server, cliNextEngineConfig }) => {

}
